<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thud.myecormerce">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".View.SplashscreenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".View.MainActivity"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />
        <activity android:name=".View.CategoryActivity" />
        <activity android:name=".View.ProductDetailActivity" />
        <activity android:name=".View.OrderDateilsActivity" />
        <activity android:name=".View.DeliveryActivity" />
        <activity
            android:name=".View.AddAddressActivity"
            android:theme="@style/Dark" />
        <activity android:name=".View.MyAddressActivity" />
        <activity android:name=".View.ViewAllActivity" />

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />

        <activity android:name=".View.PaymentDetailsActivity" />
        <activity android:name=".View.UpdateUserInforActivity" />
        <activity android:name=".View.SearchActivity" />
        <activity android:name=".View.NotificationActivity"></activity>
    </application>

</manifest>